from setuptools import setup, find_packages

from gui_template._version import __version__


setup(name='gui_template',
      version=__version__,
      description='',
      author='??',
      packages=find_packages(),
      include_package_data=True)
